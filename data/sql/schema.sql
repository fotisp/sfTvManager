CREATE TABLE preferences (id BIGINT AUTO_INCREMENT, name VARCHAR(255) NOT NULL, value text NOT NULL, created_at DATETIME NOT NULL, updated_at DATETIME NOT NULL, PRIMARY KEY(id)) ENGINE = INNODB;
CREATE TABLE season (id BIGINT AUTO_INCREMENT, number VARCHAR(255) NOT NULL, start_date DATE, feed TEXT NOT NULL, regex text NOT NULL, series_id BIGINT NOT NULL, created_at DATETIME NOT NULL, updated_at DATETIME NOT NULL, INDEX series_id_idx (series_id), PRIMARY KEY(id)) ENGINE = INNODB;
CREATE TABLE series (id BIGINT AUTO_INCREMENT, name VARCHAR(255) NOT NULL, created_at DATETIME NOT NULL, updated_at DATETIME NOT NULL, PRIMARY KEY(id)) ENGINE = INNODB;
ALTER TABLE season ADD CONSTRAINT season_series_id_series_id FOREIGN KEY (series_id) REFERENCES series(id) ON DELETE CASCADE;
